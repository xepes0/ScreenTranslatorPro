name: Build unsigned IPA

on:
  push:
    branches:
      - main
      - dev/ipa-mvp
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Generate project
        run: xcodegen generate

      - name: Build unsigned app
        run: |
          set -o pipefail
          xcodebuild \
            -project ScreenTranslatorPro.xcodeproj \
            -scheme ScreenTranslatorPro \
            -configuration Release \
            -destination 'generic/platform=iOS' \
            -derivedDataPath DerivedData \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY="" \
            build | tee xcodebuild.log

      - name: Package IPA
        run: |
          APP_PATH="DerivedData/Build/Products/Release-iphoneos/ScreenTranslatorPro.app"
          test -d "$APP_PATH"
          rm -rf Payload ScreenTranslatorPro-v0.2.0-beta6-unsigned.ipa
          mkdir -p Payload
          cp -R "$APP_PATH" Payload/
          ditto -c -k --sequesterRsrc --keepParent Payload ScreenTranslatorPro-v0.2.0-beta6-unsigned.ipa

      - name: Upload IPA
        uses: actions/upload-artifact@v4
        with:
          name: ScreenTranslatorPro-v0.2.0-beta6
          path: |
            ScreenTranslatorPro-v0.2.0-beta6-unsigned.ipa
            xcodebuild.log
          if-no-files-found: error
